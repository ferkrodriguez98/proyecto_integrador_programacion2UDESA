<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.x.x/dist/alpine.min.js" defer></script>
</head>
<body class="bg-gray-300 flex flex-col h-screen justify-between">
    <%- include('../common/header.ejs') %>
    <div class="w-full px-4">
        <% for (let i = 0; i < results.length; i++) { %>
            <!-- <div class="w-full flex border py-2">
                <div class="w-1/12">
                    foto
                </div>
                <div class="w-2/12">
                    <a href="/users/<%= results[i].username %>">
                        <h1 class="">
                            <%= results[i].username %>
                        </h1>
                    </a>
                </div>
                <div class="w-6/12 sm:w-4/12">
                    <%= results[i].email %>
                </div>
                <div class="hidden md:flex w-2/12">
                    <% if (results[i].birthdate != null) { %>
                        <%= moment(results[i].birthdate).format('DD/MM/YYYY') %>
                    <% } else { %>
                        unknown
                    <% } %>
                </div>
                <div class="w-3/12">
                    <%= results[i].favorite_genre %>
                </div>
            </div> -->
            <div class="w-full md:w-1/2 max-w-sm bg-white rounded shadow mx-auto my-6 py-12 mx-4">
                    <div class="text-center w-full">
                        <a href="/users/<%= results[i].username %>">
                            <h1 class="pt-2 ">
                                <%= results[i].username %>
                            </h1>
                        </a>
                    </div>
                    <div class="pt-6 text-center w-full">
                        <h2>
                            <%= results[i].email %> 
                        </h2>
                    </div>
                    <div class="pt-6 flex w-full text-center">
                        <h3 class="pt-2 mx-auto w-1/2">
                            <%= results[i].birthdate %> 
                        </h3>
                        <h3 class="pt-2 mx-auto w-1/2">
                            <%= results[i].favorite_genre %> 
                        </h3>
                    </div>
                </div>
           <% } %>
    </div>
   
   <%- include('../common/footer.ejs') %>
</body>
</html>